# Verifica della distribuzione

Aggiornamento 1.9.1: i rifiuti HTTP dei server LLM conservano ora il dettaglio JSON sicuro restituito dal provider, con rimozione di chiavi e header Bearer. I rifiuti transitori ricevono un tentativo limitato; incompatibilità esplicite del reasoning, dello schema JSON e del budget di contesto usano una variante locale senza alterare il profilo salvato. Se LM Studio dichiara `No models loaded`, l’app chiama il suo endpoint nativo `/api/v1/models/load`, usando il modello selezionato e l’eventuale contesto configurato. La validazione dei piani parziali usa il numero finale delle scene, mentre i nomi di ruolo copiati nel campo `scene_type` vengono adattati deterministicamente. Il caso reale Ulisse è ripartito dagli stessi checkpoint e ha completato 10/10 scene: 8 mappe, 10 rotte geografiche, una sequenza di tappe e 10 scene con personaggi; la sceneggiatura è quindi iniziata. La stessa V2 si è fermata prima della voce sulla nuova revisione visuale: 3 immagini automatiche disponibili, miniature servite nella pagina del progetto e nella gestione drag and drop, nessun placeholder. La colonna dei riferimenti è stata verificata a 380 px nella pagina reale, con separatore trascinabile, limite adattivo e preferenza persistente. **158 test automatici superati.**

Aggiornamento 1.9.0: aggiunta una pausa visuale facoltativa dopo asset e mappe e prima del TTS. Il comando di anteprima può costruire una timeline provvisoria senza audio; l’approvazione applica ritratti, luoghi e sfondi caricati e riprende dai checkpoint della voce. I riferimenti obbligatori possono essere esclusi e ricevono un placeholder neutro quando restano attivi senza immagine; i suggerimenti partono disattivati e possono essere attivati con placeholder o immagine. Le scene storiche senza mappa espongono i primi sfondi suggeriti, fusi dietro il contenuto editoriale. Verificati migrazione del database, persistenza nelle versioni V2/V3, blocco della ripresa generica durante la revisione, approvazione, esclusione e riattivazione, sfondo senza riquadro, preservazione dei testi, timeline stimata, API e sintassi JavaScript. **154 test automatici superati.**

Aggiornamento 1.7.1: un dossier locale singolo fino a 60.000 caratteri viene fornito integralmente all’autore, evitando che il top-k semantico concentri il piano su pochi episodi; raccolte più grandi mantengono il RAG ibrido. La pianificazione richiede copertura cronologica, più capitoli e scene distinte, mentre un controllo deterministico rifiuta ripetizioni sostanziali nella scaletta e nella voce. Chatterbox e i server TTS esterni usano una sceneggiatura da circa 112 parole al minuto e non vengono accelerati oltre 1,15×; se una voce rimane più lenta, il film può estendersi invece di deformarla. Le coordinate esplicite nei documenti correggono i luoghi omonimi nei pack generali, e i WAV Higgs con intestazioni streaming vengono normalizzati prima del riuso. L’interfaccia mostra il tempo di elaborazione totale, cumulato attraverso interruzioni e riprese, e la barra laterale può essere compressa con preferenza persistente. **141 test automatici superati.** Nel browser locale sono stati verificati menu aperto/compresso, persistenza dopo la riapertura e tempo recuperato del progetto Ulisse: 1 h 35 min. Il progetto concluso non è stato rigenerato.

Aggiornamento 1.7.0: i progetti appartengono ora a una famiglia con numero di versione. Rigenerare un risultato completato crea V2/V3 conservando record e file precedenti; rigenerare un tentativo non completato archivia workspace, checkpoint, ricerca, audit ed eventi e riusa lo stesso identificatore. Riprendi continua a usare l'endpoint e i checkpoint preesistenti. L'eliminazione è limitata ai progetti inattivi e rimuove record, eventi e cartella privata. Il client Higgs esegue un controllo `/status` dopo ogni comando del modello e l'interfaccia presenta il risultato come stato dichiarato dal server, senza dedurre l'uso reale della VRAM. **134 test automatici superati.**

Aggiornamento 1.6.2: rimosse le tre convalide che bloccavano i riferimenti Higgs oltre 30 secondi nella preparazione del progetto, nella prova voce e nella registrazione persistente. Resta il controllo comune della libreria H3: WAV PCM da 4 a 60 secondi. Un test d'integrazione invia un campione di 31 secondi attraverso entrambi gli endpoint Higgs simulati. **130 test automatici superati.**

Aggiornamento 1.6.1: l'Admin riapre il profilo TTS configurato o quello appena salvato, mantenendo visibile il suo endpoint reale. Il riconoscimento dei viaggi comprende anche rientri, traversate e spostamenti; nelle regie cartografiche le scene di evento e riepilogo mantengono la mappa di orientamento, mentre la sequenza narrativa è confinata in una fascia compatta sopra la timeline principale. Il progetto Ulisse completato non è stato rigenerato. **129 test automatici superati**, più una prova reale e deterministica di fotogrammi 1920×1080 senza TTS o MP4.

Aggiornamento 1.6.0: il renderer cartografico usa ora un color grading stabile del rilievo, fiumi e pin ad alto contrasto, territori con bordo gerarchico, nastri di movimento e terminali semantici distinti per fenomeni militari e civili. Le unità sono badge illustrati e i ritratti evitano i luoghi focali. Chatterbox e i server TTS scrivono un manifesto verificato che collega ogni frase al proprio WAV; il motore di timeline non può più tentare il fallback Piper dopo una sintesi esterna riuscita. La prova Higgs accetta il nome profilo omesso usando il nome del provider. La produzione reale di Ulisse viene usata per controllare anteprime, rendering completo e assenza di tremolii. **126 test automatici superati.**

La prova reale di Ulisse ha ripreso i dodici WAV Chatterbox già generati, creato la timeline senza caricare Piper e completato 6 scene in 180,067 secondi. Il video finale contiene 5.402 fotogrammi, H.264 1920×1080 a 30 fps, AAC stereo 48 kHz, sottotitoli e 6 capitoli. La decodifica integrale è riuscita; non risultano intervalli neri e il controllo dei lampi ha misurato un salto interno massimo di 2,1935, distinguendo i tagli tra scene. Loudness −16,03 LUFS, picco reale −1,40 dBTP. SHA-256: `54ef9beddf3ffab450603fb21e634b9f774184f18eebe92624eaf6cd3842c48c`. Il film e i documenti usati restano nei dati locali esclusi dalla distribuzione.

Aggiornamento 1.5.0: il client Higgs segue la specifica remota con `GET /v1/status`, `POST /v1/model/load`, `POST /v1/model/unload`, cloning multipart tramite `reference_audio` e `reference_text`, e registrazione facoltativa tramite `/v1/voices/upload`. Il modello viene caricato una volta per l’attività vocale e scaricato nel blocco finale. WAV, MP3, FLAC e OGG vengono normalizzati localmente. Le immagini facoltative dei personaggi hanno ora lookup bilingue e fallback procedurale dichiarato; i comandanti delle battaglie conservano il controllo rigoroso precedente. **123 test automatici superati.**

Aggiornamento 1.4.1: corretto il caso reale `hisarlık` restituito da Gemma come ID geografico. Gli ID tecnici di luoghi, persone, entità ed eventi vengono convertiti in forma ASCII stabile prima della validazione, senza cambiare nomi, coordinate o contenuti visibili. Collisioni dopo la normalizzazione restano errori espliciti. La risposta reale salvata nel progetto di Ulisse ora produce `hisarlik` e supera il catalogo. **119 test automatici superati.**

Aggiornamento 1.4.0: aggiunti profili separati per server TTS OpenAI compatibili, Higgs, ElevenLabs e Google Cloud. Le credenziali sono cifrate con Windows DPAPI, mentre ogni progetto congela soltanto la configurazione pubblica della voce. Higgs può ricevere il WAV one-shot; ogni risposta viene validata e normalizzata in WAV PCM mono a 24 kHz, quindi usa senza modifiche la cache, la timeline misurata e il montaggio esistenti. **118 test automatici superati.** I test di interoperabilità simulano i quattro contratti HTTP e non chiamano servizi a pagamento.

Aggiornamento 1.3.0: aggiunta una libreria locale **Documenti e fonti** con trascinamento di PDF, DOCX, TXT e Markdown, testo incollato, metadati modificabili e selezione per singolo progetto. L'indicizzazione usa su CPU il modello multilingue `paraphrase-multilingual-MiniLM-L12-v2`, fissato a una revisione precisa e verificato durante l'installazione; se il modello semantico non è disponibile rimane una ricerca lessicale BM25. I documenti scelti vengono copiati in modo immutabile nel progetto, citati con identificatori stabili e mai inseriti come percorsi locali nella descrizione pubblica per YouTube. I PDF scansione senza testo sono conservati con stato «serve OCR» ed esclusi dalla produzione; i vecchi file DOC devono essere salvati come DOCX. **112 test automatici superati**, oltre a una prova reale del modello RAG locale che ha recuperato il passaggio pertinente su Austerlitz.

La produzione reale di Austerlitz con Chatterbox ha generato 10 scene e un MP4 di 300,233 secondi. Il controllo finale ha inizialmente rilevato un salto di luminosità di 5,6092 all'ingresso del ritratto nell'apertura: la comparsa è stata resa graduale e le etichette delle scene 8 e 9 sono state riposizionate. Dopo il nuovo rendering delle sole scene interessate, il salto massimo è 0,6516. Il file finale è 1920×1080 a 30 fps, H.264 `yuv420p`, AAC stereo 48 kHz, con sottotitoli e 10 capitoli; tutti i 9.007 fotogrammi e l'intero audio sono stati decodificati senza errori. Loudness −16,05 LUFS, picco reale −1,43 dBTP. SHA-256: `f3176a0eab2274499587c47bad4c0f82643deffea7bd370baa74fe9e5a3d3784`.

Aggiornamento 1.2.2: corretto il passaggio **Ritratti e materiali** dei progetti Chatterbox, che non richiede più il file voce Kokoro previsto dai pack tradizionali. Il worker vocale espone il numero dei segmenti generati e l'app lo riflette nell'avanzamento della produzione. La revisione visiva ora controlla i fotogrammi al 55% e all'85% di ogni scena; l'apertura separa titolo e ritratto, mentre la chiusura conserva i simboli ma nasconde i nomi di esercito ridondanti. Aggiunta la durata selezionabile di 3 minuti. **106 test automatici superati.**

Aggiornamento 1.2.0: controllo `reasoning_effort` a tre stati e persistente per connessione, ora sempre visibile nella pagina Admin. Sul server LM Studio reale configurato, `none` ha prodotto la risposta breve in 3,5 secondi; `medium` ha consumato l'intero budget sperimentale di 64 token prima della risposta finale, confermando l'attivazione e il costo del ragionamento. LM Studio recente rifiuta `response_format.type=json_object`: la modalità JSON ora invia lo schema Pydantic effettivo come `json_schema`, seguendo il contratto ufficiale. Aggiunto il recupero controllato delle risposte in cui piccoli modelli collocano i valori nel contenitore `properties` dello schema. Per le battaglie, un nuovo passaggio semantico separa direzione, tipo di movimento e fazione dalle coordinate: genera frecce e unità anche quando l'outline iniziale è incompleto. Le coordinate più usate sono ricontrollate con richieste Nominatim limitate, sequenziali e memorizzate. La camera distingue ora l'inquadratura regionale dalle viste tattiche.

Aggiornamento 1.2.1: i risultati Nominatim omonimi oltre 80 km dalla coordinata locale dichiarata vengono scartati anche quando provengono da un checkpoint precedente. La cartografia delle battaglie usa patch con risoluzione indipendente, fino allo zoom 15 per le inquadrature tattiche, e curve di livello raster stabili. Il posizionamento preventivo include nomi dei luoghi, contatori e nomi delle unità. La revisione visiva ferma il lavoro soltanto per difetti gravi associati a una scena identificabile; le osservazioni generiche vengono registrate come avvisi.

Chatterbox Multilingual V3 è ora un motore effettivo della pipeline e non più soltanto una prova da terminale. La scelta è salvata per progetto; l’Admin accetta campioni WAV PCM da 4 a 60 secondi, ne controlla formato e impronta, e il progetto conserva una copia immutabile. Il worker carica il modello una volta, genera le frasi mancanti e alimenta la timeline misurata esistente usando chiavi di cache verificate contro il motore. Cambiare voce conserva i checkpoint editoriali e geografici e invalida soltanto voce e fasi successive. I vecchi progetti migrano esplicitamente a Kokoro. L’installer include Chatterbox per impostazione predefinita, con esclusione volontaria `-SenzaChatterbox`; l’app rifiuta una produzione Chatterbox se runtime o pesi non sono presenti, senza fallback silenzioso. **104 test automatici superati.** La precedente prova CPU reale del modello resta il riferimento prestazionale; durante questa modifica non è stata avviata una nuova sintesi lunga. La prova reale Waterloo con Gemma 4 12B Q6 ha completato 10 scene su 10 e fornito casi reali per il recupero dello schema e la correzione della resa cartografica.

Aggiornamento 1.1.6: profili persistenti per più server locali o remoti. La migrazione legge il precedente formato a connessione singola; ogni URL normalizzato conserva provider, ultimo modello, parametri e chiave DPAPI separata. Le API pubbliche restituiscono soltanto `has_api_key`, mai il segreto. Verificati cambio server, ritorno al primo server senza reinserimento, cambio del solo modello, separazione delle chiavi e assenza dei segreti nel JSON su disco. Controllata anche la sintassi dell'interfaccia JavaScript. **90 test automatici superati.**

Aggiornamento 1.1.5: regia visuale dei viaggi, sequenza narrativa animata per tappe non localizzate, ritratti automatici in riquadro, timeline letteraria e patch cartografiche dettagliate. Il preflight misura il contenuto realmente visibile e interrompe mappe vuote o componenti incompleti prima della voce. Il battle outline usa catalogo e gruppi di due scene, con normalizzazione univoca e diagnostica precisa dei riferimenti. La stessa implementazione è testata attraverso endpoint locali e remoti compatibili simulati; nessuna API a pagamento viene chiamata.

La prova grafica reale genera quattro fotogrammi 1920×1080 usando il motore, l'atlante e un ritratto Commons già acquisito: apertura geografica, due istanti distinti della sequenza e arrivo. Gli stessi timestamp sono deterministici e la sequenza cambia nel tempo. È una prova di rendering, non una ricostruzione storica o un MP4 completo.

**88 test automatici superati.** Due prove aggiuntive hanno usato realmente `google/gemma-4-12b`, già servito gratuitamente da LM Studio su localhost. Il piano di viaggio è stato completato dai checkpoint in due richieste e 49,79 secondi: 4 scene di mappa, 4 movimenti, 2 sequenze di tappe e 4 scene con personaggi. Il piano tecnico di Waterloo è stato completato in 4 richieste e 167,66 secondi: 6 luoghi, 3 comandanti, 4 scene e 4 percorsi; una risposta con quattro scene al posto di due è stata corretta automaticamente. La successiva ripresa ha completato lo stesso piano senza chiamare il modello. Le prove con endpoint remoti sono simulate sul medesimo client e contratto, per non chiamare API a pagamento. Questi controlli verificano interoperabilità e struttura visuale; non sono una verifica storica indipendente e non producono due nuovi filmati.

Aggiornamento 1.1.4: corretto il passaggio degli ID geografici `focus` dal compilatore generale al contratto degli effetti del motore. Riparazione automatica e reversibile dei pack già salvati, con controllo CLI prima della preparazione di mappe e asset. **80 test automatici superati**: inclusi compilatore reale → validazione completa, tutti i pack generali distribuiti, compatibilità Waterloo, effetti preesistenti, dati ambigui, copia di sicurezza e ripresa che conserva checkpoint e mappe senza chiamare il modello. Renderer, TTS e validatore battle mantengono i byte di riferimento.

Riprodotto inoltre il traceback segnalato su una copia separata di un progetto salvato di dieci scene. Il file riparato supera la validazione CLI sia del motore precedente copiato dal workspace sia di quello aggiornato. Generati dieci fotogrammi reali 1920×1080, deterministici allo stesso timestamp; ispezionate a vista le scene 01, 02 e 10. Il confronto dei dati conferma che sono cambiati soltanto i focus duplicati; narrazione, fonti, coordinate, camere e checkpoint originali sono conservati. L'installazione di test e il suo progetto sono stati soltanto letti, senza modifiche. La prova verifica il difetto di interoperabilità: non è una nuova valutazione storica o della qualità cartografica del piano, non scarica i ritratti e non produce un nuovo MP4. Nessuna chiamata LLM o sintesi vocale è stata eseguita. I dati del progetto e le anteprime restano locali, esclusi dal repository e dallo ZIP.

Aggiornamento 1.1.3: piani generali costruiti per passaggi e gruppi di due scene, divisione automatica dei gruppi troncati, normalizzazione senza invenzione di riferimenti geografici e diagnostica durante le richieste LLM. 65 test automatici superati, inclusi troncamento, risposte incomplete, limite delle richieste, ripresa dopo disconnessione, errori semantici nei luoghi e negli eventi e continuità degli stati territoriali. Nessuna modifica al renderer o al TTS.

Eseguita anche una prova reale e gratuita con `google/gemma-4-12b` già installato in LM Studio su localhost. Il primo passaggio ha salvato concetto e catalogo, ma ha evidenziato un tipo evento inventato (`literary_narrative`). Dopo aver aggiunto vocabolario esplicito e diagnostica mirata, la ripresa ha prodotto quattro scene valide in due richieste, circa 100 secondi complessivi, conservando concetto e catalogo. Il tema era il ritorno di Ulisse; il piano dichiara `literary_tradition`. Questa prova valida l'interoperabilità e il contratto dei dati, non le coordinate o i fatti storici, e non produce un nuovo video. L'API DeepSeek configurata nell'installazione di test non è stata chiamata. Audit, checkpoint e risultati della prova rimangono locali e non sono distribuiti.

Aggiornamento 1.1.2: ricerca ibrida predefinita, con modalità rigorosa opzionale. 53 test automatici superati: nessuna fonte, fonti parziali, errore di rete, interruzione, ripresa del vecchio blocco, entrambe le famiglie di documentario, correzione editoriale, riferimenti inventati e conservazione della provenienza. Ricerca web e LLM sono simulati in queste prove; compilatori ed esportazione dei documenti sono reali. Verificati nel browser il salvataggio delle modalità e l'avviso nel progetto, usando dati di prova separati. Renderizzate e ispezionate due anteprime reali 1920×1080, mappa e confronto, con fonti vuote e fotogrammi identici allo stesso timestamp. Il vecchio pack Waterloo supera ancora la validazione. Nessuna nuova produzione completa MP4 né prova con un LLM remoto per questa correzione. Renderer, TTS e validazione battle rimangono invariati; il registro conserva gli hash originali e dichiara le estensioni a compilazione/validazione dei documenti generali.

Aggiornamento 1.1.1: comandi locali START.bat e STOP.bat, con AVVIA.bat compatibile. 37 test automatici superati; avvio reale sulla porta predefinita e su una porta alternativa, arresto selettivo senza chiudere l'altra istanza, arresto completo e stop ripetuto su server gia fermo verificati. Il processo viene identificato attraverso interprete, percorso esatto di run.py e data di creazione, senza affidarsi soltanto al PID salvato. Lo stop chiude anche i processi figli del server; progetti e output salvati rimangono sul disco.

Aggiornamento 1.1.0: libreria immagini e riquadri, 35 test automatici, controlli nel browser e MP4 dimostrativo reale. Vedi [Immagini e riquadri](IMMAGINI.md) per il rapporto aggiornato. I controlli seguenti documentano la distribuzione iniziale 1.0.0.

Prima installazione autonoma verificata su Windows x64, Intel i7-1165G7 e 16 GB di RAM, senza GPU NVIDIA.

- Scaricati nella nuova cartella: uv 0.12.9, Python 3.13.13 gestito, dipendenze fissate, Kokoro e voci. Python, eSpeak e FFmpeg esterni non sono stati usati.
- Installazione e controlli di coerenza dei due ambienti completati.
- 25 test superati nella copia clonata: API, segreti, endpoint, coda, compatibilità storica, distribuzione, cache geografica e riferimenti delle immagini.
- Interfaccia iniziale e amministrazione controllate nel browser; voce e motore risultano installati. Il modello remoto resta da configurare.
- Produzione dimostrativa realmente completata in 1920×1080 a 24 fps: 2.884 fotogrammi, circa due minuti; decodifica completa, audio, sottotitoli, capitoli e variazioni di luminosità controllati.
- I 17 moduli del motore sono stati confrontati con la pipeline funzionante prima del confezionamento. Nessuna riscrittura del renderer o del TTS; impronte originali in `engine-baseline.json`. Git può normalizzare i terminatori di riga senza modificare il codice.

Il filmato usa **ricerca e risposte LLM preparate esplicitamente per il test**, non un server LLM reale. Cartografia, download, sintesi vocale, rendering, montaggio e verifica del file sono reali. Nessuna prova viene presentata come una produzione storica verificata da un modello remoto.

La prova Chatterbox CPU è stata eseguita nell'ambiente sperimentale originario. Dalla versione 1.2.0 lo stesso runtime è integrato nella selezione voce dell’app e nella pipeline; i pesi restano esclusi dallo ZIP sorgente e vengono acquisiti dall’installer con revisioni e checksum registrati.

La clonazione da Git è stata eseguita in una cartella separata con spazi nel percorso, senza hard link al repository sorgente. AVVIA.bat ha installato autonomamente tutti i componenti con un PATH privo di Python e Git, usando il proprio runtime gestito. Avvio su porta alternativa e secondo avvio della stessa istanza superati. È una prova isolata sullo stesso PC, non un test su ogni possibile installazione di Windows.

Il video dimostrativo dura 120,1667 secondi, contiene quattro capitoli, sottotitoli italiani e audio AAC stereo a 48 kHz. Loudness misurata: −16,01 LUFS; picco reale −3,79 dBTP. Differenza delle durate audio/video inferiore a un millisecondo. SHA-256: `8623150b105bc1f90b7b377a884830ba88ba4501fc6c089277e43429311a9836`.

Lo ZIP è prodotto dal commit corrente, riaperto e controllato per integrità. Runtime, modelli, cartelle private, database, cache e video di prova sono esclusi. I due BAT nello ZIP mantengono i terminatori Windows. Lo SHA-256 dell'archivio e il commit sorgente sono nel JSON affiancato allo ZIP.

Il repository è disponibile su GitHub come `emanuelealbertosi/H3-documentary`. I risultati di ogni nuova esecuzione CI sono consultabili nella scheda Actions; le verifiche locali descritte sopra restano distinte dalla CI remota.
